import 'package:flutter/material.dart';

class AnimeStickerLoader extends StatelessWidget {
  const AnimeStickerLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Anime Sticker Loader'),
    );
  }
}
